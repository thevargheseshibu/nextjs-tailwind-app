/** @type {import('next').NextConfig} */
const nextConfig = {
    // next.config.js
    images: {
        domains: ['media.istockphoto.com','www.etpgroup.com'],
      },
};

export default nextConfig;
